services:
  kanban:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - kanban-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/kanban.db
      - ROCKET_ADDRESS=0.0.0.0
      - ROCKET_PORT=8000
    restart: unless-stopped

volumes:
  kanban-data:
