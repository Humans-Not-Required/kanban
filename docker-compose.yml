services:
  kanban:
    build: .
    ports:
      - "${PORT:-8001}:8001"
    volumes:
      - kanban-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/kanban.db
      - ROCKET_ADDRESS=0.0.0.0
      - ROCKET_PORT=8001
    restart: unless-stopped

volumes:
  kanban-data:
